{
  "scripts": {
    "test": "ava --verbose"
  },
  "devDependencies": {
    "ava": "^0.15.2",
    "faker": "^3.1.0",
    "lodash": "^4.14.1",
    "supertest": "^2.0.0",
    "supertest-as-promised": "^3.2.0"
  },
  "name": "node-assessment",
  "description": "* The file must be called server.js\r * There is a package.json included with dependencies in it, you should know what to do.\r * It must use express, and body parser (Set up the app and use the body parser)\r * You will want to require the included `users.json file`  `var users = require('./users.json');`\r * At the bottom of the file you must `module.exports` your express app.\r * Listen on port 3000.",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "test": "^0.6.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DevMountain/node-assessment.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/DevMountain/node-assessment/issues"
  },
  "homepage": "https://github.com/DevMountain/node-assessment#readme"
}
